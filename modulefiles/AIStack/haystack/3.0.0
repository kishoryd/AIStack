#%Module1.0
#
# AIStack :: Haystack 3.0.0
# RAG
# CUDA: 13.0 (cu130 wheel index)
#

module-whatis "AIStack Haystack 3.0.0 :: RAG, CUDA: 13.0 (cu130 wheel index)"

proc ModulesHelp { } {
    puts stderr ""
    puts stderr "  Haystack 3.0.0"
    puts stderr "  Category : RAG"
    puts stderr "  CUDA     : 13.0 (cu130 wheel index)"
    puts stderr "  Prefix   : /home/apps/miniconda/envs/haystack"
    puts stderr ""
    puts stderr "  This module does not change your shell prompt. To confirm it loaded:"
    puts stderr "    echo \$CONDA_DEFAULT_ENV"
    puts stderr "    which python"
    puts stderr ""
}

# One conda env active at a time: loading another MLDL/AIStack module
# swaps this one out instead of stacking PATH entries.
family "condaenv"

set envpath /home/apps/miniconda/envs/haystack

if { ![file isdirectory $envpath] } {
    puts stderr "AIStack/haystack/3.0.0: $envpath does not exist -- run install_aistack.sh first."
    break
}

setenv       CONDA_PREFIX       $envpath
setenv       CONDA_DEFAULT_ENV  haystack
setenv       CONDA_SHLVL        1
setenv       VIRTUAL_ENV        $envpath
prepend-path PATH               $envpath/bin
